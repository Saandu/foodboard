/**
 * Languages the dashboard and public pages are translated into.
 *
 * This is the admin interface locale, not the set of languages a restaurant
 * publishes its menu in — that is configured per structure and can include
 * languages this list does not cover.
 *
 * Labels are endonyms so each option reads correctly whatever locale is active.
 */
export const UI_LANGUAGES = [
  { value: 'en', label: 'English' },
  { value: 'ro', label: 'Română' },
  { value: 'it', label: 'Italiano' }
]
